export const ASSISTANT_API_ENDPOINT = "/api/chat";

export const ASSISTANT_STATUS = {
  enabled: false,
  message:
    "Assistant backend is intentionally deferred for now. The UI/endpoint boundary is reserved for a future phase.",
};
